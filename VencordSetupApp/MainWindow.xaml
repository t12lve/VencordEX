<Window x:Class="VencordSetup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Installation de VencordEX" 
        Height="420" Width="580"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="app.ico"
        ShowInTaskbar="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Grid Margin="12">
        <Border CornerRadius="20" Background="#101218" BorderBrush="#252936" BorderThickness="1.2">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#5865F2" Opacity="0.35"/>
            </Border.Effect>

            <Grid Margin="26,22,26,22">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 1. En-tête -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="app.ico" Width="22" Height="22" Margin="0,0,10,0"/>
                        <TextBlock Text="VENCORDEX" FontWeight="Black" FontSize="14" Foreground="#8da0f9"/>
                        <TextBlock Text=" • ASSISTANT D'INSTALLATION" FontWeight="Bold" FontSize="14" Foreground="#4e5366" Margin="4,0,0,0"/>
                    </StackPanel>

                    <Button Click="BtnClose_Click" Grid.Column="1" Width="26" Height="26" Background="#1c1f2b" BorderBrush="#2e3347" BorderThickness="1" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                    <TextBlock Text="✕" FontSize="12" Foreground="#949ba4" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- 2. Contenu des étapes -->
                <Grid Grid.Row="1">

                    <!-- ÉTAPE 1 : Configuration et choix du dossier -->
                    <StackPanel x:Name="StepConfigGrid" Visibility="Visible" VerticalAlignment="Center">
                        <TextBlock Text="Installer VencordEX dans Program Files" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,4"/>
                        <TextBlock Text="Installe VencordEX de manière permanente comme une application Windows native." FontSize="12.5" Foreground="#949ba4" Margin="0,0,0,18"/>

                        <!-- Sélecteur de chemin -->
                        <TextBlock Text="DOSSIER DE DESTINATION" FontSize="10.5" FontWeight="Bold" Foreground="#656d8a" Margin="0,0,0,6"/>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#161822" CornerRadius="8" BorderBrush="#2c3247" BorderThickness="1" Padding="10,6">
                                <TextBox x:Name="TxtInstallPath" Text="C:\Program Files\VencordEX" 
                                         Background="Transparent" BorderThickness="0" Foreground="#FFFFFF" FontSize="12.5" VerticalAlignment="Center"/>
                            </Border>

                            <Button Click="BtnBrowse_Click" Grid.Column="2" Height="36" Padding="14,0" Background="#1c1f2b" BorderBrush="#2e3347" BorderThickness="1" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <TextBlock Text="Parcourir..." FontSize="12" Foreground="#b5bac1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>

                        <!-- Options -->
                        <StackPanel Margin="0,0,0,10">
                            <CheckBox x:Name="ChkPatchShortcuts" IsChecked="True" Margin="0,0,0,8">
                                <TextBlock Text="Mettre à jour les raccourcis Discord pour l'auto-patch Vencord" FontSize="12" Foreground="#b5bac1"/>
                            </CheckBox>
                            <CheckBox x:Name="ChkCreateSettingsShortcut" IsChecked="True" Margin="0,0,0,8">
                                <TextBlock Text="Créer le raccourci VencordEX Paramètres dans le menu Démarrer" FontSize="12" Foreground="#b5bac1"/>
                            </CheckBox>
                            <CheckBox x:Name="ChkRegisterUninstall" IsChecked="True">
                                <TextBlock Text="Enregistrer dans Applications et fonctionnalités (Désinstallation facile)" FontSize="12" Foreground="#b5bac1"/>
                            </CheckBox>
                        </StackPanel>
                    </StackPanel>

                    <!-- ÉTAPE 2 : Progression de l'installation -->
                    <StackPanel x:Name="StepProgressGrid" Visibility="Collapsed" VerticalAlignment="Center">
                        <TextBlock x:Name="LblProgressStatus" Text="Copie des fichiers dans Program Files..." FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,6"/>
                        <TextBlock x:Name="LblProgressSub" Text="Veuillez patienter quelques instants..." FontSize="12.5" Foreground="#949ba4" Margin="0,0,0,16"/>

                        <Border Height="12" Background="#161822" CornerRadius="6" BorderBrush="#202433" BorderThickness="1">
                            <Grid ClipToBounds="True">
                                <Border x:Name="InstallProgressBar" HorizontalAlignment="Left" Width="0" CornerRadius="5">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#5865F2" Offset="0.0"/>
                                            <GradientStop Color="#854FEE" Offset="0.5"/>
                                            <GradientStop Color="#57F287" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- ÉTAPE 3 : Terminé avec succès -->
                    <StackPanel x:Name="StepSuccessGrid" Visibility="Collapsed" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Border Width="34" Height="34" CornerRadius="17" Background="#238636" Margin="0,0,12,0">
                                <TextBlock Text="✓" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Installation terminée avec succès !" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF"/>
                                <TextBlock Text="VencordEX est prêt à l'emploi dans Program Files." FontSize="12.5" Foreground="#57F287"/>
                            </StackPanel>
                        </StackPanel>

                        <Border Background="#141722" CornerRadius="10" Padding="14" BorderBrush="#202535" BorderThickness="1" Margin="0,10,0,14">
                            <StackPanel>
                                <TextBlock Text="EMPLACEMENT :" FontSize="9.5" FontWeight="Bold" Foreground="#656d8a"/>
                                <TextBlock x:Name="LblFinalPath" Text="C:\Program Files\VencordEX" FontSize="12" Foreground="#FFFFFF" Margin="0,2,0,8"/>
                                <TextBlock Text="• Discord démarrera désormais automatiquement avec VencordEX." FontSize="11.5" Foreground="#949ba4"/>
                                <TextBlock Text="• Vous pouvez désinstaller à tout moment depuis les Paramètres Windows." FontSize="11.5" Foreground="#949ba4" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                </Grid>

                <!-- 3. Boutons d'action en bas -->
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <!-- Bouton Installer (Étape 1) -->
                    <Grid x:Name="BtnGroupConfig" Visibility="Visible">
                        <Button Click="BtnInstall_Click" Height="42" Background="#5865F2" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="10">
                                        <TextBlock Text="⚡ Installer VencordEX" FontSize="13.5" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="b" Property="Background" Value="#4752C4"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <!-- Boutons Fin (Étape 3) -->
                    <Grid x:Name="BtnGroupSuccess" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Click="BtnLaunchDiscord_Click" Grid.Column="0" Height="42" Background="#5865F2" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                        <TextBlock Text="🎮 Lancer Discord" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="BtnFinish_Click" Grid.Column="2" Height="42" Background="#1e2230" BorderBrush="#2c3247" BorderThickness="1" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                        <TextBlock Text="Terminer" FontSize="13" FontWeight="SemiBold" Foreground="#b5bac1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Window>
